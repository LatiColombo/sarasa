package ar.com.ada.online.second.abstracts.discount;

import java.util.ArrayList;

public class RejunteBasura {

    /*

    // en Game había hecho esto:

    private ArrayList<Spell> spells = new ArrayList<>(); //inicializo Spell para no ponerlo en el constructor
    public static Integer attakSpellQuantity = 0;
    public static Integer defenseSpellQuantity = 0;


    public void addSpell (Spell spell) {
        if (Spell instanceof AttackSpell) {
            attakSpellQuantity++;
        }
        this.spells.add(spell);

        if (Spell instanceof DefenseSpell) {
            defenseSpellQuantity++;
        }
        this.spells.add(spell);
    }


    // en Wand hice esto intentando el Random:
        private static Wand wand = new Wand();
    public static void wandList(String[]) {
        List<String> list = new ArrayList<>();
        list.add("Sauco");
        list.add("Olivo");
        list.add("Pino");
        list.add("Abedul");
        list.add("Roble");

        for (int i = 0; i < 5; i++) {
            getRandomWand(list);
        }
    }

    private static void getRandomWand(List<String> list) {
        int index = wand.nextInt(list.size());  //no sé por qué no me toma "nextInt"

    }
     */
}
